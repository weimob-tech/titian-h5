
# 组件名中文 _组件名英文_
**组件描述信息**

## 安装使用
```typescript showLineNumbers
import { xxx } from 'titian-h5-react'
```

## 用法示例

#### 基础用法
```typescript tsx showLineNumbers
const App: React.FC = () => {
  return (
    <>
      <xxx />
    </>
  )
}
```
#### 不同用法名称
#### 不同用法名称
#### 不同用法名称

## XXX API
### 属性 **Properties**
| 名称 | 类型 | 必填 | 默认值 | 说明 | 备注 |
| ---- | ---- | ---- | ------ | ---- | ---- |

### 事件 **Events**
| 名称 | 参数列表 | 描述 | 备注 |
| ---- | -------- | ---- | ---- |

### 插槽 **Slots**
| 名称 | 说明 | 备注 |
| ---- | ---- | ---- |

### 外部样式类 **External Classes**
| 名称 | 说明 | 备注 |
| ---- | ---- | ---- |

### CSS 变量 **CSS Variable**
| 变量 | 默认值 | 说明 | 备注 |
| ---- | ------ | ---- | ---- |
